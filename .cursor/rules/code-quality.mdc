---
description: 代码质量 - 防止屎山代码
alwaysApply: true
---

# 代码组织（单文件内分区）

按模块用注释分区，顺序固定：配置区 → 数学解析 → 变换链构建 → Canvas 渲染 → 动画引擎 → UI 控制 → KaTeX 渲染。

# 质量底线

- **单一职责**：一个函数只做一件事，超过 ~50 行考虑拆分。
- **禁止复制粘贴**：重复逻辑必须抽成函数；魔法数字提取到配置区常量。
- **命名清晰**：用有意义的中文/英文语义名，杜绝 a1、tmp2 之类。
- **状态集中**：动画/视图状态统一放在一个 state 对象，避免散落全局变量。
- **注释解释"为什么"**，而非复述代码在做什么。
